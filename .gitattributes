# Shell scripts must keep LF line endings so they run on Linux/macOS even when
# edited or committed from Windows.
*.sh text eol=lf

# PowerShell scripts are Windows-side; keep CRLF.
*.ps1 text eol=crlf

# Docs: normalise to LF.
*.md text eol=lf
